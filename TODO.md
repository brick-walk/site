# GitHub Site TODO
- No GitHub Pages settings changes are required if your repository is already deploying from `main` and `/ (root)`.
- Run `npm run build` before pushing so the generated top-level HTML pages stay in sync with `templates/` and `src/pages/`.
- Commit the generated HTML files (`index.html`, `about.html`, `portfolio.html`, `project.html`, `contact.html`, `faq.html`, `commissions.html`) whenever page/template changes are made.
