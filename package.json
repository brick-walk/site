{
  "name": "portfolio-template",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run build && serve . -p 3000"
  },
  "dependencies": {
    "serve": "^14.2.6"
  }
}
